define([ "tools/three.require", "tools/camera", "tools/container" ], function(THREE, camera,
		container) {
	// var controls = new THREE.OrbitControls( camera, container );
	/* var controls = new THREE.OrbitControls(camera);
	controls.noKeys = true;
	controls.target = new THREE.Vector3(0, 0, 10*40);
	return controls;*/
});